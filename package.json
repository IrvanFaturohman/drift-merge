{
  "name": "drift-merge",
  "private": true,
  "version": "0.1.0",
  "description": "2D top-down idle racing prototype: auto-drifting cars, NOS taps, merge & circuit upgrades.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && gh-pages -d dist --nojekyll -m \"Deploy to GitHub Pages\""
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^8.3.0"
  },
  "dependencies": {
    "@fontsource/fredoka": "^5.3.0"
  }
}
